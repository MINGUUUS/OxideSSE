# Structure examples

Add one or two small CIF/POSCAR files here, such as a representative LLZO-derived structure.

Suggested filenames:

- `LLZO.cif`
- `POSCAR_LLZO`

These files can be used with:

```python
import oxidesse as sse
sse.optimize_structure("examples/structures/LLZO.cif")
```
